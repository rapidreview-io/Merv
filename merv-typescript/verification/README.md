# Verification records

Reports are dated evidence for the implementation that existed when they ran;
their test counts and integration catalogs are not a current inventory.

`living-research.json` records the Paper, Reflections, Consolidation and Research
integration: 681 passing tests, one optional skip, 39 successful synthetic HTTP
tool calls through the complete cycle, production-bundle browser acceptance,
lost-response recovery and cleanup. It also records the independent synthesis
authority finding and fix. This is not central Git publication or full Python parity.

`experiments.json` records the production Experiment lifecycle, seven tools,
scoped recovery and immutable evidence, actual UI/Cordis removal/restoration,
independent review and 610 passing tests. `experiments-live.json` records four
fresh native stages, 24 successful MCP calls, five shell calls, independent
recomputation and cleanup. It distinguishes the original launched script from
the corrected post-run verifier. The tiny synthetic study proves its calculation
and lifecycle; corpus, Reflection and consolidation/publication remain next.

`review-returns.json` records optional explicit review destinations, immutable
storage/replay, domain-owned route validation, Task compatibility, UI rendering
and 574 passing tests. `review-returns-live.json` preserves the two fresh native
reviewers' seven successful MCP calls and exact retained routes, with independent
transcript/database checks and cleanup. The owner and evidence are synthetic;
that historical run did not cover the production Experiments program. The separately audited
Python Experiment reference has 41 passing tests.

`step-07-*.json` records the former two-tool Nisa REST adapter, removed on
2026-09-14 after verifying the newer Nisa-owned MCP integration. Those reports
are preserved unchanged for audit history. Their removed scripts and credentials
preflight are no longer operational instructions.

The Nisa MCP path is documented in [configuration and migration](../docs/NISA_PLUGIN.md)
and [implementation and limits](../docs/NISA_PLUGIN_IMPLEMENTATION.md).
`nisa-plugin-integration.json` and `nisa-plugin-checkpoint.json` retain the original
2026-09-13 six-tool implementation evidence.

`nisa-rest-removal.json` records replacement verification before deletion and the
full post-removal Merv suite with the cross-repository scenario enabled.

`workflow-tools-removal.json` records the interface at the time after retiring the
workflow adapter: 22 native tools and a 29 → 23 → 29 Nisa mount cycle. The earlier
Nisa removal report describes the preceding cleanup and retains its original counts.

`mounted-tool-names.json` records the subsequent naming change to
`_<mountId>.<upstreamToolName>`. Earlier records intentionally retain the
`mount__<id>__<tool>` names used when those checks ran.

`workflow-guidance.json` records the first shared guidance implementation.
`task-closure.json` records the subsequent producer/operator closure and managed
version upgrade slice, its full suite and separate live/browser/Fable outcomes.
The current inventory is 37 domain tools, or 39 with the two optional UI tools. The network has 40 plugins and 84 direct dependencies, including 21 service providers and 37 default configuration entries; historical report counts below are unchanged.

`workflow-guidance-live.json` is the sanitized successful three-agent acceptance
run across two server restarts. Raw transcripts stay outside the repository.

`work-item-dependencies.json` records the subsequent durable work DAG and task
prerequisite integration, including its Python comparison and context retry fix.

`structured-task-evidence.json` records the versioned evidence contract and context replay/reference integration, including 236 passing checks and local review fixes. `structured-task-evidence-live.json` is its sanitized fresh three-agent acceptance run across two restarts, with both work and review context builds.

`review-assessments.json` records richer verdict reporting, waiver compatibility and 251 passing checks. `review-assessments-live.json` preserves sanitized evidence from the fresh three-agent run, including comparison of the actual submitted findings against stored results. The Fable item distinguishes completed generic design consultation from the separately blocked private-source audit.

`workflow-assignment.json` records 274 passing tests, actual concurrent begin validation and the unchanged plugin network. `workflow-assignment-live.json` records three fresh agents, four begin calls and exactly two persistent start events across two restarts. These cover interactive activation; sessions, runner and research-program parity remain open.

`actor-credentials.json` records the separate project-bound credential lifecycle,
293 passing tests and 137 Python identity/session audit checks. Its Fable entry
distinguishes the completed generic design consultation from a private-source
audit. `actor-credentials-live.json` records fresh agent execution across token
rotation and server restarts. Shared human accounts and sessions were still open
at that checkpoint.

`shared-identity.json` records the independent verifier and Scope-owned project
membership integration: 343 passing tests, recovery/admission fencing after access
loss, eleven synthetic browser-client cases and a source-free Fable consultation.
`shared-identity-live.json` records three fresh agents using signed user identities,
explicit project selection and role/evidence isolation across two projects and
two server restarts. Real shared Supabase/Nisa login has not been verified;
user-owned machine keys and sessions were separate follow-up work at that checkpoint.

`user-keys.json` records the integrated owner-bound project/account key lifecycle,
369 passing tests, actual competing writer transactions, machine write provenance,
HTTP/MCP/UI and delayed mounted-dispatch guards. `user-keys-live.json` records the
fresh three-agent task loop through key rotation and server restart, plus owner
management after departure and recursive lineage revocation. These keys do not
implement exact-node sessions or a runner; real shared-provider login remains
unverified.

`workflow-execution.json` records the fixed-policy foundation: 386 passing tests,
strict argument bindings, durable policy/absence hashes, generation fencing,
metadata-only admission and a canonical HTTP/MCP description path.
`workflow-execution-live.json` records three fresh agents and the actual fixed
assignment metadata they received. It explicitly retains the timing difference
before the final removal of interactive begin from node grants. These checks do
not establish real session credential enforcement; ordinary keys remain broad.

`sessions.json` records 410 passing checks for actual assignment-scoped MCP
credentials, immutable source/assignment leases, metadata activation, writer and
mounted-call guards, worker output authority, and durable recovery. Its Fable
entry records a rejected consultation pending specific payload approval, not a
completed review. `sessions-live.json` records two fresh agents under one source
key completing 17 successful calls across two restarts. It distinguishes the
production Sessions control plane from the separate acceptance harness and the
still-missing production Runner.

`dispatch.json` records metadata-only workflow candidates, workspace declarations,
source-bound runner presence/settings, transactional automatic lease selection,
project pause/halt and the optional Sessions UI. `dispatch-live.json` retains the
two-agent automatic HTTP assignment proof and its timing limits. Native machine
process and Git workspace lifecycle remain open.

`runner.json` records 464 passing checks and the independent native machine Runner.
`runner-live.json` records two fresh native agents, two local shell commands,
nine successful MCP calls, independent review and confirmed process cleanup.
The final deterministic suite includes a later source-versus-session refusal fix.
Git workspaces and the remaining production runner operations are still open.

`workspaces.json` records the subsequent private Git workspace lifecycle, immutable
server attachments/results, real Git recovery tests and native profile isolation
audit. `workspaces-live.json` records two fresh agents editing and inspecting the
same captured commit, with three shell commands, two successful MCP calls and
confirmed cleanup. Its first attempt exposed a test-program permission error;
the corrected managed workflow retained explicit producer/reviewer permissions.
The final automated suite also covers later declared-workspace UI corrections.
This proof uses one machine and a synthetic workflow; independently reviewed
publication, cross-machine objects and the research programs remain open.

`code-operations.json` records 526 passing checks, durable live checkpoint commands,
fixed Runner Git execution, crash/replay fencing, Code plugin unload/reload and
actual UI component rendering. `code-operations-live.json` records two fresh native
agents, six shell commands, six successful MCP calls and an actual independent
Reviews assessment of the exact producer receipt. Both workers and workspaces
closed, the reviewer checkout was deleted, the command was acknowledged and the
source/private central stayed unchanged. Production proposal tools, central
publication and cross-machine transport remained open at that checkpoint;
older records retain their original source hashes and historical limits.

`code-proposals.json` records 553 passing checks, immutable Code sealing with
original worker/receipt/evidence identity, generic Reviews verdict routing and
preserved Tasks replay, real proposal unload/reload and Code UI rendering.
`code-proposals-live.json` records two fresh native agents, five shell commands and
eight successful MCP calls: producer artifact creation, production Code sealing,
both pinned artifact reads and production `review.submit` dispatch. The producer
recovered from a sandbox-blocked heredoc; no MCP call failed. This is the latest
checkpoint. The synthetic domain is not the production experiment/reflection or
consolidation program, and the retained source hashes do not assert otherwise.

`claims.json` records 566 passing tests for project facts, revision conflicts,
immutable command receipts, project/session authority and actual plugin removal.
It also supersedes the earlier Code-proposal post-artifact authority test's weak
exception assertion with a specific refusal and mutation-sensitive regression.
`claims-live.json` records two fresh native agents, nine successful MCP calls,
original create/update receipts on retry, independent project-scoped reading and
confirmed cleanup. This synthetic program does not implement scientific review,
Experiments or reflection publication. Browser evidence is labeled separately.

`optional-consolidation.json` records 695 passing tests and one optional skip.
Consolidation requires Code, while Research keeps an optional Consolidation
connection. New no-code cycles finish after approved reflection; Git cycles wait
for Consolidation and then resume the same handoff without duplicate children.
Persisted version-2 cycles retain their original behavior across restart and
replay. These checks use local integration fixtures, not a production migration.
