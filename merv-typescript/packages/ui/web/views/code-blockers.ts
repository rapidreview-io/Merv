/**
 * Code's person-move vocabulary lives in the shared contracts, so the server's Running
 * contribution writes the same sentence this page does. Edit it there, not here.
 */
export * from '@merv/contracts/code-blockers';
