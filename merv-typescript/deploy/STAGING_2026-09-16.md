> Superseded private-stage status: the accepted imported image and history are now running. See [REHEARSAL_2026-09-16.md](REHEARSAL_2026-09-16.md). The original baseline evidence below is retained.

# Isolated Azure staging — September 16, 2026

This is a **partial staging candidate**, not a production cutover. Historical import, a final rebuild, critical review, and real-user acceptance remain required.

## Release

- VM: `rp-control-1`, SSH alias `ResearchSuite_Control`.
- Container: `merv-typescript-control-1`, separate Compose project `merv-typescript`.
- Binding: `127.0.0.1:3081` on the VM; no public route was changed.
- Image: `merv-typescript:20260916T074217Z-be69497b-0fa8d37a518a`.
- Image ID: `sha256:70484183dd9124c48ca87cba46b99fa824d3ca1fd95aac104659dfc4d4b40ade`.
- Node base: `node@sha256:83f487e0a63425e5b4d146fb5e5be574bcbe1b7b843d3ebafdd95eaf7767a7e5`.
- Source content SHA-256: `0fa8d37a518a5a03b9e5b5af0dca1fb3be538af4f7dd7e0d872a77a4c7918711`.
- Archive SHA-256: `856d0bbae82e135fe243625454c791a9d139b2e13f59a8c5b954686be39bf650`.
- Private release directory: `/opt/merv-typescript/releases/20260916T074217Z-be69497b-0fa8d37a518a`.

The release directory contains the source manifest/archive, build log, pinned build manifest, and `staging-acceptance.json`. The image built the UI, compiled the backend and tests, prepared compiled workspace exports, pruned development dependencies, and ran the compiled CLI without TSX. Its source snapshot predates later import/readiness changes; rebuild before final acceptance.

## Storage and authentication

- New `merv_ts_app` role owns only the new `merv_ts` schema; it cannot create schemas or read any of the 56 legacy public tables.
- Application migrations: 49 applied; all 53 configured plugins activated.
- Remote objects use the separate `merv-ts` prefix. Existing Python object keys are unchanged.
- `/etc/merv/typescript.env` is root-owned, mode 0600, and contains only the required named configuration. `/var/lib/merv-ts` is owned by UID/GID 1000, mode 0700.
- Authentication is configured for the same Supabase project in HS256 mode. Public JWKS was empty, and the existing configured secret verified the existing anon key signature. This configuration check does **not** replace an actual user-login test.
- The localhost/production OAuth redirect allowlist was not inspected or changed. Do not claim Google login has passed yet.

## Acceptance observed

| Check                                                      | Result                               |
| ---------------------------------------------------------- | ------------------------------------ |
| Container health and plugin activation                     | Healthy; 53/53 active                |
| Compiled `/ui/` HTML, JavaScript and CSS                   | 200                                  |
| `/health` and shared `/auth/config`                        | 200; identity enabled                |
| Anonymous `/projects`                                      | 401                                  |
| Configured HTTPS Origin                                    | Accepted                             |
| Unapproved Origin                                          | 403                                  |
| Native PostgreSQL transaction rollback                     | Passed; no synthetic event committed |
| Real remote object put/get and duplicate conditional write | Passed                               |
| Staging-only restart and remote reread                     | Passed                               |
| Synthetic project or artifact rows created                 | None; target remained empty          |

One 88-byte synthetic object was retained under `merv-ts/staging_checks` for persistence verification. Its local receipt contains no credentials.

## Backups and preserved services

Before creating the isolated schema/role, backups were written under `/var/backups/merv/typescript-staging/20260916T073819Z-be69497b-e23e8204ae97` (directory 0700, files 0600). The manifest records checksums for the PostgreSQL custom dump, roles, Caddyfile, and existing private environment files. `pg_restore --list` passed. The custom dump was 12,018,716 bytes, SHA-256 `723b6f4f0f222c608c5081097d7d6e8dd8cb7a503ac9aabdde28ba12f2adfb34`.

The legacy backend remains `merv-control:63f94101` with its original September 12 start time. Caddy's checksum remains `34fd3b77057a85e7ce69e03bb998c14a4f21474327dd3028a9d849c51e6d5968`, identical to the saved original. No Vercel routes, legacy database schema/data, existing environment files, or old artifact prefix were changed. Sandbox/gateway/Hatchet services were not restarted.

## Private browser review

An SSH tunnel was established from workstation `127.0.0.1:3481` to VM `127.0.0.1:3081`. While it remains open, the staging UI is at `http://127.0.0.1:3481/ui/`. Its control socket is `/private/tmp/merv-ts-staging-20260916.sock`.

Close only this tunnel with:

```sh
ssh -S /private/tmp/merv-ts-staging-20260916.sock -O exit ResearchSuite_Control
```

See `README.md` for the full staged cutover and rollback procedure. Do not infer public-cutover approval from this staging report.
