# TypeScript production cutover — 16 September 2026

**Deployed at 19:48 UTC.** The TypeScript backend and revised UI now serve https://experiments.rapidreview.io/ui/. Both legacy UI entry points redirect there. Automated public checks pass; the real-account HTTPS browser confirmation requested from the user remains pending.

## Authorized scope

The user authorized the TypeScript backend/UI replacement, requested importing existing projects before switching, then explicitly directed that unfinished work be removed. No active users needed live-session continuity. The user confirmed their normal Supabase account could sign into the private preview and see their projects, and requested better UI. Standing authorization permits relevant Fable consultations.

## Release

- Image: `merv-typescript:20260916T190552Z-be69497b-3a026ebca252`.
- Image ID: `sha256:3235cf85425ff0311e58b51d856d76fb66ff80ce136e1fc75d74259e7ca07042`.
- Node base: `node@sha256:83f487e0a63425e5b4d146fb5e5be574bcbe1b7b843d3ebafdd95eaf7767a7e5`.
- VM release: `/opt/merv-typescript/releases/20260916T190552Z-be69497b-3a026ebca252`.
- Final schema: `merv_ts_prod_20260916a`; file prefix: `merv-ts/merv_ts_prod_20260916a`.
- Final original export: `python81v2-20260916T193431Z`; filtered source: `python81final-20260916T193431Z`.
- Migration tool revision: `0987b90cd969`, root-owned under `/opt/merv-typescript/cutover-tools/0987b90cd969`.

The UI release preserves all non-UI production source/configuration hashes from the previously accepted image. The offline pruning/import tools were mounted separately, with pinned hashes; the runtime image was not rebuilt during migration. [UI verification](UI_REFRESH_2026-09-16.md) and [earlier real-provider rehearsal](REHEARSAL_2026-09-16.md) retain their own evidence boundaries.

## Writer pause and backup

Maintenance began at **19:34:31 UTC**. The legacy `deploy-control-1` container was stopped, and zero `merv_app` database connections remained. A fresh full custom-format PostgreSQL backup and role export were created before filtering. Its public schema was restored into an isolated temporary database, with project, experiment, task, reflection, workflow and event counts matching. The disposable restore-check database was then removed.

Private backup: `/var/backups/merv/typescript-cutover/20260916T193431Z`. It contains old Caddy/environment files, `postgres.dump`, `roles.sql`, restore evidence, cutover receipts and exact execution scripts with SHA-256 hashes. The original database and objects remain for rollback through at least **2026-10-10**. Their retention does not expose unfinished work in the new product.

An initial preflight halted before maintenance because PostgreSQL's text form of a host address includes `/32`; the verified connection belonged to the expected legacy container. The comparison was corrected to `host(client_addr)`. No write or route change occurred in that failed preflight.

## Unfinished-work disposition

The source snapshot is immutable. Filtering produced a separate derived snapshot and per-table audit, with seven recorded detachments. Unknown workflow kinds/states, mismatched outcomes, broken retained references or removal of finished independent work fail closed.

| Records | Original | Removed from new product | Retained |
| --- | ---: | ---: | ---: |
| Projects | 30 | 0 | 30 |
| Experiments | 257 | 34 | 223 |
| Tasks | 23 | 0 | 23 |
| Reflections | 76 | 4 | 72 |
| Claims | 200 | 0 | 200 |
| Completed artifact records | 4,489 | 0 | 4,489 |
| Artifact figures | 180 | 0 | 180 |
| Historical workflow instances | 1,059 | 729 | 330 |
| Feed posts | 791 | 45 | 746 |

Closed includes successful, failed and abandoned work as defined by each legacy workflow. Associated requests, reviews, links and events for removed unfinished work are excluded. Finished independent children survive with references detached. Completed files survive even if they were only linked to discarded work. No source or destination object bytes were deleted. Historical records are read-only; no legacy leases or in-progress native workflows are recreated.

Original snapshot SHA-256: `5d53e228e6603b4c5998e194c07e784743b73f77c7f697e319b72490f7227af3`. Derived snapshot SHA-256: `7cdcdb3d2e7da55e6f0b6d1b353558fbc87669e34d02ca2c96512b394d2a019c`. Full backup SHA-256: `9177e5e4227e73d794068025a3a1b7c4880afc5cb3c92c95eb74996300294425`.

## Review and checks

- Actual `claude-fable-5` reviewed product readiness, UI, cleanup policy/implementation and final switch scripts. These were source reviews, not independent VM executions. Safe evidence and local dispositions are in `docs/reviews/`.
- All 16 deployment/import tests passed, including numeric event pointers, unfinished lens references, retained completed children and shared files. The final filter dry run also passed on the immutable full-size rehearsal snapshot.
- All 2,960 retained file objects were copied to the final prefix and independently verified by size/hash. Database reconciliation matched every native row/hash, historical row/hash, file-availability binding and import receipt. Full downloads and a 64 KiB range from the 4,877,283-byte largest retained file passed provider checks; no empty retained file exists to sample.
- Production Supabase callback `https://experiments.rapidreview.io/ui/` and the local preview callback were appended to the existing allowlist. The existing site URL, providers and other redirect entries were preserved. Real staging login was user-confirmed; final HTTPS account login remains a separate browser check.
- Both routing-only Vercel releases passed 21 preview route checks. Explicit `/merv/` handling fixes a preview 404. Installer/catalog bytes and content types are preserved; unrelated main-site routes retain their behavior.

## Client transition

See [client reconnection instructions](CLIENT_CUTOVER.md). Disable the legacy 0.1.5 Merv plugin, including old skills, and configure direct HTTP MCP with a new production machine key. Uninstall only if disabling is unavailable. Other plugins and the Supabase account remain unchanged. A new published plugin bundle is not part of this cutover.

## Rollback boundary

The Python image, stopped container, database, files and original routing are retained. Previous Vercel deployment IDs are in the routing plan/receipts. A rollback restores routes and starts the old service; it must preserve TypeScript writes and must not restore the old database over the new schema. Writes made after cutover do not automatically appear in Python.

## Public deployment result

- Final service acceptance: **19:47:46 UTC**. Forced recreation and container environment inspection confirmed the exact production schema, prefix and import source. All **54 plugins active**, 30 projects, 43 memberships, 3,222 native readable artifacts, 200 claims and **46,889 history rows**. The member/project/role tuples exactly match the user-verified rehearsal. Native workflows and sessions both start at zero; no unfinished historical workflow remains in the derived source.
- The 3,222 readable artifacts consist of 2,938 original retained artifacts plus 284 derived figure/feed artifacts. The 1,551 historical metadata-only artifact records remain labeled without fabricated download links, covered by the preexisting pinned audit; no newly missing file was accepted. Original artifact history retains all 4,489 complete records.
- Public Caddy switch: **19:48:14 UTC**. `/` redirects to `/ui/`; health, UI, auth configuration and both exact compiled assets pass over HTTPS. Anonymous project/MCP requests return 401. Approved Origin passes; unapproved Origin returns 403. Security headers remain. Sandbox routing is byte-identical. Final Caddy SHA-256: `18681d07a41ce9c9d5726e74a324f2176ae6bb1daf0397291d9d300c0eeb1d34`.
- Direct legacy UI alias promoted to `dpl_H8hFbXSkJ9XSBQeyaJjsVRoYg9Jq` (`research-suite-nzclqt4px-guraltoos-projects.vercel.app`), preserving rollback `dpl_HXBM7C2DWX6Qbi56JJTDsJy96J9m`.
- Main site alias promoted to `dpl_xhUHGiWA1atW7Pfowf4koiMxtguV` (`rr-site-qpa5lxc8b-guraltoos-projects.vercel.app`), preserving rollback `dpl_3a7H7GTTvweXJdtydrT5W5s2wcbn`.
- All **21 public route checks** passed after promotions, including unchanged legacy installer/catalog bytes and unrelated main-site pages. The exact five-redirect configurations were also synchronized into `research_state_ui/vercel.json` and the RR_Site checkout; no other configuration fields changed. These local changes were not committed or pushed.
- At **19:50 UTC**, TypeScript and PostgreSQL were healthy with zero container restarts; legacy remained stopped. The new browser login page loaded email/password and Google options. No user credential was entered by the agent; final user-reported HTTPS sign-in is still pending.

Safe machine-readable evidence: [CUTOVER_2026-09-16.json](CUTOVER_2026-09-16.json). Protected VM evidence: `final-stage-acceptance.json` and `public-acceptance.json` in the backup directory, import/reconciliation/provider receipts under `/var/lib/merv-legacy-exports/python81final-20260916T193431Z/`.

This cutover does not claim complete Python feature parity, a published replacement client plugin, migrated external mount/runner credentials, or freedom from future defects.
