# Living research acceptance — 2026-09-15

This verifies the user-authorized living-research wave. It does not claim full Python parity, scientific validation of the synthetic fixture, central Git publication, or production upstream deployment. Machine-readable evidence is in [living-research.json](../verification/living-research.json).

| Requirement                                                      | Current implementation and evidence                                                                                                                                                                                                                                                                    |
| ---------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Remote compute/storage remains delegated                         | Existing External Mounts/Scope grants and upstream credentials remain the integration boundary. No compute or heavy-storage provider was added. Existing transport and sandbox-mount regressions pass in the full suite; no cloud resources were provisioned.                                          |
| Token-saving artifact transfer only                              | `artifact-upload` reads a bounded local file and calls existing `artifact.create` through MCP. It validates returned hash/size and prints a compact receipt; the agent need not reproduce file bytes. Two real tests cover bounds, normal session authority and refusals. [Usage](ARTIFACT_UPLOAD.md). |
| Keys now; OAuth later                                            | Existing project/account keys, Scope authority and assignment credentials remain in use. Full key/session/API regressions pass. No new OAuth work or real-provider deployment is claimed.                                                                                                              |
| Reflection is a plugin using ordinary Workflows                  | Reflections registers parent/lens definitions, frozen Knowledge/Paper inputs, five independent children, synthesis and independent review. Tests exercise all leased stages, both repair routes, contributor exclusion, exact context and Cordis unload/reload.                                        |
| Consolidation follows approved reflection and other dependencies | Consolidation pins the exact approved reflection and additional workflow prerequisites. Tests cover one decision per frozen experiment, lease recovery and a real Code commit/sealed proposal whose independent reviewer receives the exact head.                                                      |
| Literature and structured project definition                     | Paper owns problem/scope/goals/constraints, ordered sections, citations, links, revision CAS and retained history. Tests cover rollback, tenant boundaries and restart; actual browser forms saved all four definition fields and a linked citation.                                                   |
| Living Methods/Results                                           | Separate writing workflows freeze research and coordinator-provided artifacts. Tests and the HTTP acceptance run publish both exact document revisions with source hashes, coverage and freshness. Browser checks confirmed publication views and stale marking after a problem edit.                  |
| Project guidance as an outer workflow                            | Research connects existing prerequisite workflows, Reflection, Consolidation and both Paper updates through real dependency edges. Full-chain tests reach complete only after both exact publications, and verify child creation/replay/rollback across restart.                                       |
| Fully integrated interfaces                                      | Default configuration includes each provider, tools and optional UI adapter. Boundary, HTTP/MCP, lifecycle and browser checks passed. Portable UI data models avoid server runtime imports.                                                                                                            |

## Verification results

- Full regression: **682 tests; 681 passed, zero failed, one optional Nisa-checkout integration skipped** (`MERV_NISA_CHECKOUT` unset).
- Backend build, browser typecheck and production UI build pass. The UI build reports its existing-style bundle-size advisory; no runtime failure occurred.
- A synthetic real HTTP-tool run made **39 successful calls** across five lens authors, synthesis, independent review, consolidation, independent review, both paper publications and outer completion. Each publication retained four exact downstream source/evidence artifacts.
- Browser acceptance exercised the actual production bundle: editing, tab preservation, citations and section links, reflection details, consolidation provenance, both published documents, read-only access and complete-cycle guidance. The main acceptance tab had no browser error logs.
- A loopback proxy replaced one successful `paper.patch` reply with HTTP 500. The form stayed locked and retained the exact original request across a tab change and a refresh. Retrying sent identical bytes and recovered revision 5; it did not create revision 6. The changed project definition correctly marked older publications stale.
- Independent source review found and reproduced a synthesis authority mismatch. Admission now consistently requires the verified wave owner or an operator, including delegated workers. Its new regression passes; no remaining blocker was identified in the bounded goal review.
- The disposable demo, fault proxy and acceptance tabs were closed. No production data, cloud resources, external model agents or upstream service accounts were changed by this acceptance run.

The full suite log is `/private/tmp/merv-living-acceptance-suite.log`; the retained JSON summarizes its outcome and scope. [Remaining parity](REMAINING_PARITY.md) keeps later publication, candidate and operational work separate from this completed implementation wave.
