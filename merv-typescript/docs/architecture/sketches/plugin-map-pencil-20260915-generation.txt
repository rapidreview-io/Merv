Tool: built-in Image Gen
Source: current-dependencies.json regenerated from source on 2026-09-15
Final: plugin-map-pencil-20260915.png

Use case: infographic-diagram.
Draw a technically accurate, beautifully hand-drawn PENCIL ARCHITECTURE DEPENDENCY MAP on a warm ivory sketchbook page. Wide landscape, high resolution, extremely readable. Fine graphite strokes, subtle imperfect pencil outlines, neat architect's handwritten lettering, light cross-hatching and paper grain, small spiral-binding holes at far left. Flat top-down scan with no perspective distortion. Predominantly graphite, no colorful UI, no photorealistic objects obscuring diagram. Lots of breathing room. This is a BIG PICTURE plugin map, not a workflow sequence or tool catalog.

TITLE exactly: "MERV — THE PLUGIN MAP"
Subtitle: "Current TypeScript / Cordis architecture"
Use a faint large enclosing boundary labelled "MERV SERVER · Cordis manages activation and unloading".
Use these SIX primary server cluster boxes, plus a small Mounts box. Every named service listed below is a real independent plugin; a large outline is only a visual grouping. Do not imply a cluster is itself one plugin.
Layout: slim FRONT DOOR across upper-left/top, ACCESS upper-right, three main boxes across middle (RESEARCH at left, COORDINATION at center, EVIDENCE at right), broad FOUNDATION below. Put MOUNTS at far upper-right edge beside ACCESS. Put a separate RUNNER machine outside server at lower-left and a separate EXTERNAL SERVICES box outside server at far right. Design clean intentional routes with visible arrowheads and no lines over lettering.

BOX CONTENT EXACT:
FRONT DOOR
"API · Tools · UI"
small subtitle "HTTP / MCP and browser"
small outlined inset "23 adapters connect domain plugins to API, Tools and UI"

RESEARCH
"Claims — research statements"
"Tasks — managed work"
"Experiments — plans, attempts & results"
"Knowledge — research inventory & snapshots"

COORDINATION
"Workflows — gates & next actions"
"Sessions — assignments & recovery"
"Code — commits & capture references"

EVIDENCE
"Artifacts — file metadata"
"Reviews — independent verdicts"
"Context Builder — starting context"
"Feed — project activity"

ACCESS
"Identity — verify shared users"
"Access — tool grants"
"Credentials — upstream secrets"

FOUNDATION
"State — durable records"
"Scope — actors, projects & permissions"
"Blobs — file bytes"
"Domain Events — durable events"
Small pencil annotation below State and Scope: "The two most shared dependencies"
Small annotation: "Scope → State   ·   Domain Events → State"
Small annotation: "Artifacts → Blobs (file storage)"

MOUNTS
"Mounts"
"Optional remote tools"

RUNNER outside boundary, label "SEPARATE MACHINE"
"Runner"
"Launches agents & manages Git checkouts"

EXTERNAL SERVICES outside boundary
"Nisa MCP"
"Sandboxes MCP"
"Remote services, not local Cordis plugins"

ARROW SEMANTICS: Solid pencil arrows A→B ALWAYS mean A depends on B. Group arrow means one or more plugins in A directly require a plugin in B, NOT that every member depends on every other member. Main exact grouped dependencies:
RESEARCH → COORDINATION (label "workflow & code")
RESEARCH → EVIDENCE (label "evidence, reviews, context")
COORDINATION → EVIDENCE (label "Code uses Artifacts")
COORDINATION → ACCESS (label "Sessions uses Access")
MOUNTS → ACCESS (label "grants & credentials")
MOUNTS → FRONT DOOR (label "Tools registry")
FRONT DOOR → ACCESS (label "identity & grants")
Draw four neat downward dependency branches to FOUNDATION from RESEARCH, COORDINATION, EVIDENCE and FRONT DOOR. They may share a clearly labelled dependency rail "Shared State / Scope dependencies" to reduce clutter, but remain visibly connected and have arrowheads toward FOUNDATION. ACCESS → FOUNDATION is labelled "Scope". Avoid more individual foundation arrows.
For adapter wiring, the inset inside FRONT DOOR has a thin bracket labelled "Domain adapters" visually spanning the three middle domain clusters; this is an explicitly labelled aggregation, NOT a reversed dependency arrow. Caption at that bracket "Adapter → its owner + registry". The direct base-provider arrows above still apply.
Two DASHED network connections, visually distinct from dependency arrows:
RUNNER ↔ FRONT DOOR labelled "authenticated HTTP · Sessions / Code controls"
MOUNTS ↔ EXTERNAL SERVICES labelled "MCP"
Do NOT draw any solid Cordis dependency from Runner. Runner is independently configured on its machine.

Bottom-left legend:
"Solid → requires"
"Dashed ↔ network connection"
"Names = plugins · Boxes = groups"
Bottom-right note:
"23 service plugins + 23 adapters"
"Grouped view; internal edges condensed"
Tiny light pencil note outside main graph: "Next: Reflection → consolidation → publication (not implemented)"
Accuracy constraints: do not put Reflection in the implemented server. Do not suggest Context Builder depends on Tasks or Experiments. Do not show Blobs depending on State. Do not show Scope depending on Identity. No older Nisa REST plugin. No invented plugins, tools or dependency arrows. Prioritize legible names, correctly directed arrows, cluster relationships, generous layout and authentic sketchbook pencil aesthetic over decoration.

--- First edit ---

Edit this diagram. Preserve all plugin names, grouping, layout, useful labels and hand-drawn pencil sketchbook style. Make the paper LIGHT warm ivory with even diffuse illumination, and all pencil lettering and strokes dark charcoal with excellent contrast. Remove ALL dark vignette/shadows/black corners. It must look like a clean bright flatbed scan of a sketchbook, not a dim photograph.

Correct these precise dependency arrows:
1. There is an incorrect vertical arrow from ACCESS down into EVIDENCE. DELETE that arrow entirely. Replace with a clearly separate line FROM the lower-right of ACCESS, routing DOWN through the empty corridor to the RIGHT of EVIDENCE, then entering FOUNDATION at its upper-right edge with one arrowhead pointing into FOUNDATION. Label this line "Scope". Access depends on Scope, not Evidence.
2. Above RESEARCH and EVIDENCE there is a horizontal bridge currently with downward arrowheads at BOTH boxes. Remove the arrowhead entering RESEARCH. Make the bridge originate at the top-right of RESEARCH, rise slightly, travel right, and end with ONE downward arrowhead entering the TOP-LEFT of EVIDENCE. Label "evidence, reviews, context". Meaning RESEARCH requires EVIDENCE.
3. The diagonal arrow labelled "Sessions uses Access" must visibly START at the TOP-RIGHT corner of COORDINATION, and END at the LOWER-LEFT corner of ACCESS. It must NOT start from or join the Research-to-Evidence bridge. Separate these strokes with enough whitespace; if they cross, use a small bridge/hop without a junction.
4. Preserve the horizontal RESEARCH → COORDINATION arrow labelled "workflow & code". Preserve COORDINATION → EVIDENCE labelled "Code uses Artifacts".
5. Preserve MOUNTS → FRONT DOOR, MOUNTS → ACCESS, FRONT DOOR → ACCESS, all shared-foundation dependency branches, and dashed Runner/FrontDoor and Mounts/External network connections.
6. Remove the big dashed outer MERV SERVER rectangle so external services cannot appear enclosed as local Cordis plugins. Keep the title "MERV SERVER · Cordis manages activation and unloading" above the local server boxes, and keep RUNNER marked SEPARATE MACHINE and EXTERNAL SERVICES marked remote services, not local Cordis plugins. Keep all individual group boxes and their locations.
No new content or extra arrows. Make every arrow endpoint clear. Large readable writing. Strong contrast. Bright warm ivory page.

--- Final edit ---

Make ONLY two precise line-routing corrections to this 1536x1024 pencil diagram. Preserve all text, page brightness, layout, boxes and every other arrow.
1. The diagonal line labelled "Sessions uses Access" currently starts on the Research→Evidence horizontal bridge near (884,381). That is wrong. Erase this diagonal line only. Draw a new continuous pencil arrow that clearly STARTS on the TOP edge of the COORDINATION box at (910,422), goes vertically up to (910,361), then diagonally up-right to END with an arrowhead entering the lower-left side of ACCESS at (1001,315). Where this new line crosses the Research→Evidence horizontal bridge at y382, draw a small semicircular hop, NOT a joined intersection. The source must be physically attached to COORDINATION and nowhere else. Keep its "Sessions uses Access" label.
2. The line labelled "Scope" is wrongly hidden through the EVIDENCE box. Completely erase the current ACCESS-to-FOUNDATION Scope line segments at x1290 above and below Evidence. Replace with ONE continuous line starting on ACCESS right border (1246,305), going right to (1337,305), straight DOWN to (1337,675) in the narrow OUTSIDE corridor between Evidence and External Services, left to (1300,675), then DOWN to the TOP border of FOUNDATION (1300,700), ending with arrowhead into Foundation. This line must NEVER touch or pass behind EVIDENCE (whose right border is x1322). Place label "Scope" along the upper part of this new route.
No other edits. Arrow endpoints are critical.
